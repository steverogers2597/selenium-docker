# selenium-docker
Selenium Docker is an automated test suite for web application testing using Selenium WebDriver 4 and TestNG. It implements the Page Object Model pattern to test flight reservation workflows including user registration, flight search, and booking confirmation with data-driven testing via JSON files.
